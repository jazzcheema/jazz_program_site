# Claude Development Framework

A production-ready workflow framework for AI-assisted development with Claude Code. Built on YAGNI principles, emphasizing simplicity, maintainability, and session continuity.

## Overview

This framework provides:
- **Structured workflow** with 6 core commands
- **Session continuity** across context resets and conversation limits
- **Quality assurance** through automated compliance checks
- **Team collaboration** with seamless handoffs via SESSION.md
- **Code examples** demonstrating best practices

## Philosophy

> **Build simple, maintainable software that solves real problems.**

### 1. YAGNI (You Aren't Gonna Need It)
- The best code is no code
- Build only what's needed NOW
- No speculative features
- Delete rather than comment out
- Question every abstraction
- **No safety fallbacks** - Fail fast and fix properly rather than mask issues
- **No mock implementations** - Build real features or use clearly marked stubs

### 2. Simplicity First
- **Simple > Clever** - Maintainability beats performance
- **Readable > Concise** - Clarity beats brevity
- **Explicit > Magic** - Obvious beats elegant
- **One Developer Test** - Can a single developer understand and modify this?

### 3. Chain of Thought with Verification
- Break complex problems into explicit steps
- **STOP and verify** working functionality before proceeding
- Provide **success criteria** for external tools
- Show reasoning explicitly
- Document the "why" in SESSION.md
- Never skip ahead assuming previous steps worked

### 4. Modular but Not Over-Engineered
- Clean interfaces between components
- No unnecessary abstractions
- Modules should be independent
- Avoid dependency chains
- Balance flexibility with YAGNI

## Quick Start

1. **Install Claude Code**
   ```bash
   npm install -g @anthropic-ai/claude-code
   ```

2. **Copy framework to your project**
   ```bash
   # Copy the .claude directory to your project root
   cp -r .claude /path/to/your/project/
   ```

3. **Start developing**
   ```bash
   cd /path/to/your/project
   claude
   /start
   ```

4. **Read the quick start guide**
   - [.claude/QUICKSTART.md](.claude/QUICKSTART.md) - Get productive in 15 minutes

## Framework Structure

```
.claude/
├── CLAUDE.md                    # Core personality, rules, and principles
├── WORKFLOW.md                  # Command reference and patterns
├── QUICKSTART.md                # 15-minute onboarding guide
│
├── commands/                    # 6 core slash commands
│   ├── start.md                 # Initialize/resume session
│   ├── checkpoint.md            # Save semantic progress
│   ├── spec.md                  # Interactive feature design
│   ├── plan.md                  # Convert SPEC to TODO tasks
│   ├── audit.md                 # Compliance & quality check
│   └── debug.md                 # Systematic error analysis
│
└── examples/                    # Code pattern guides
    ├── naming-guide.ts          # Good vs bad naming
    ├── testing-guide.test.ts    # Real data emphasis
    ├── stubs-guide.ts           # Proper stub marking
    └── file-structure-guide.ts  # Headers, comments, architecture
```

## Core Commands

### Session Management
- **`/start`** - Load context and begin work
- **`/checkpoint`** - Save progress to SESSION.md (every 30 min)

### Planning
- **`/spec`** - Interactive Q&A to create feature specification
- **`/plan`** - Convert SPEC.md to TODO.md task checklist

### Quality Assurance
- **`/audit`** - Comprehensive compliance and documentation check
- **`/debug`** - Structured root cause analysis

### Native Features
- **`Esc Esc`** (or `/rewind`) - Quick undo (built-in to Claude Code)

## Typical Workflow

### Feature Development
```bash
/spec           # Design feature interactively → SPEC.md
/plan           # Break down into tasks → TODO.md
/start          # Load context and begin
# Work on tasks (marked with ← CURRENT)
/checkpoint     # Save progress every 30 min
/audit          # Check compliance when complete
```

### Daily Work
```bash
/start          # Restore SESSION.md context
# Work on current task
/checkpoint     # Save progress regularly
```

### Debugging
```bash
/debug          # Systematic error analysis
# Apply fix
/checkpoint     # Document solution
```

## Understanding Checkpoints

### Two Complementary Systems

**Native Checkpoints** (`Esc Esc` or `/rewind`)
- Automatic file tracking by Claude Code
- Current session only (30-day retention)
- Quick undo and exploration

**Semantic Checkpoints** (`/checkpoint`)
- Manual context in SESSION.md
- Forever (in git)
- Session transitions and team handoffs

**They work together:** Use `Esc Esc` or `/rewind` for tactical recovery, `/checkpoint` for strategic context preservation.

## Key Files

### Generated by Commands
- **SPEC.md** - Feature specifications (from `/spec`)
- **TODO.md** - Task checklist (from `/plan`, `← CURRENT` marks active task)
- **SESSION.md** - Current state and context (from `/checkpoint`)

### Framework Files
- **[CLAUDE.md](.claude/CLAUDE.md)** - Development philosophy and rules
- **[WORKFLOW.md](.claude/WORKFLOW.md)** - Complete command reference
- **[QUICKSTART.md](.claude/QUICKSTART.md)** - 15-minute team onboarding

## Code Examples

The `.claude/examples/` directory provides comprehensive guides:

- **naming-guide.ts** - Purpose-based vs implementation-based naming
- **testing-guide.test.ts** - Real data usage, when to ask for mocks
- **stubs-guide.ts** - Proper STUB marking with TODO and timeline
- **file-structure-guide.ts** - 2-line headers, current behavior comments

## Best Practices

1. **Start every session with `/start`** - Automatic context loading
2. **Checkpoint every 30 minutes** - Session continuity
3. **Use `Esc Esc` freely** - Quick undo without fear
4. **Study examples** - Reference `.claude/examples/` when unsure
5. **Run `/audit` regularly** - Catch violations early
6. **Document in SESSION.md** - "For Next Session" enables recovery


## Documentation

- **[QUICKSTART.md](.claude/QUICKSTART.md)** - Get started in 15 minutes
- **[CLAUDE.md](.claude/CLAUDE.md)** - Development philosophy and rules
- **[WORKFLOW.md](.claude/WORKFLOW.md)** - Complete command reference and patterns
- **[examples/](.claude/examples/)** - Code pattern guides

---

**Built with YAGNI, maintained with simplicity, designed for one developer to understand.**

# jazz.cheema

Personal programming portfolio built with Next.js, React, and Three.js. The current direction is a set of quiet interactive rooms: pale software fields, sparse object staging, block-particle text, minimal controls, and purposeful hidden transitions.

## Vision

The site should feel like a small system of connected instruments rather than a game menu or marketing page. Each route gets one clear interaction:

- **Home**: a pale room with the lamp as the persistent mark and the flying-carpet object as the navigation surface.
- **Clouds**: an external-project launcher. 3D portfolio objects sit above a bottom-centered project signal, description, stack/type details, and particle controls (`<`, `VISIT`, `>`).
- **Sand**: an abstract sand-kingdom gate field. A block must be moved through a minimal castle pattern before contact is revealed.
- **CV**: the document-like portfolio page, keeping its pale editorial surface and measured type system.
- **Krate signal lab**: the hidden dark room. This is the exception where the palette can turn evil, dense, and signal-heavy.

## Stack

| Layer | Tech |
|---|---|
| Framework | Next.js 16 App Router |
| UI | React 19, Tailwind CSS 4 |
| 3D | Three.js |
| Language | TypeScript |

## Getting Started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Scripts

```bash
npm run dev    # local dev server
npm run build  # production build
npm run start  # serve production build
npm run lint   # lint
```

## Project Structure

```text
app/           # Next.js App Router pages, layouts, and client components
public/        # Static assets: models, textures, fonts, icons
```

## Design Direction

Primary internal references are the **CV page** and **Krate signal lab**. External references are **Fors** for pale restraint, tiny controls, centered signal compositions, and negative space; and **Teenage Engineering** for product-as-interface staging, thin schematic language, and instrument-like interaction.

The target is not arcade, racing, military HUD, or dense game UI. The interactive parts can still be playful, but the visual language should stay calm, precise, and sparse.

### Core Rules

- **One clear focus per page**: avoid adding explanatory panels or decorative systems unless they serve the main interaction.
- **Pale field first**: default scene surface is `#e9e5e0`; dark UI belongs mainly to the hidden Krate room or inverse hover states.
- **Lamp as site mark**: do not add large homepage identity text, portfolio-axis branding, or top-right personal labels. The lamp owns that corner.
- **Portfolio links matter most on Clouds**: preserve the bottom-centered particle project title, description, stack/type metadata, and `VISIT` link.
- **Sand stays abstract**: no genie, camera object, webcam/skyfeed, character scene, or top nav. It is a block/gate puzzle that reveals contact.
- **Buttons flip with purpose**: hover/focus should invert color and swap command text when useful, for example `MAKE_CONTACT` to `EMAIL`.
- **Block language is shared**: pixel dots, particle letters, square gates, signal grids, and small block trails are the common interaction vocabulary.
- **Use viewport corners deliberately**: controls can live at the edges when useful; avoid forcing every page into a vertical center aisle.
- **No decorative atmosphere**: avoid starfields, vortexes, generic glows, and mood effects unless the specific interaction requires them.
- **Scene pages are viewport-locked**: home, Clouds, Sand, and hidden lab should not scroll. CV can remain document-like.

### Palette

| Name | Value | Role |
|---|---|---|
| FIELD | `#e9e5e0` | Primary pale room surface |
| INK | `#161616` | Primary text and active inverse controls |
| RULE | `rgba(22,22,22,0.12)` | Dividers, grid lines, inactive frames |
| GHOST | `#8b8780` | Secondary labels and quiet metadata |
| MUTED | `#3d3a36` | Readable body copy |
| HEAT | `#c87820` | Sand/castle blocks, peak/high state |
| OK | `#249958` | Gate, ready, confirmed state |
| TRACE | `#2a5fc0` | Active block, selection, movement trail |

### Page Notes

- **Clouds** should keep the 3D object carousel visually lighter on desktop so objects do not collide with the project title and text.
- **Sand** should be very simple, but not free. Contact is earned by solving a small spatial interaction.
- **Krate hidden room** can remain theatrical and darker because the contrast is the point of that route.
- **README is the source of design truth** for this experimental redesign pass; remove old visual references when they stop matching the site.
